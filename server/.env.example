# ===========================================
# MusicVista Server Environment Variables Example
# ===========================================
# Copy this file to .env and fill in actual values

# Runtime environment
NODE_ENV=development          # Development: development, Production: production

# Server configuration
PORT=3001                     # Server port number
FRONTEND_URL=http://localhost:5173  # Frontend application URL (for CORS)

# JWT authentication configuration (⚠️ Use strong random secrets in production)
JWT_ACCESS_SECRET=your-access-secret-here     # Access token secret (recommended 64+ characters)
JWT_REFRESH_SECRET=your-refresh-secret-here   # Refresh token secret (recommended 64+ characters)
JWT_ACCESS_EXPIRATION=15m     # Access token expiration time
JWT_REFRESH_EXPIRATION=7d     # Refresh token expiration time

# PostgreSQL database configuration
DB_HOST=your-database-host.amazonaws.com  # Database host address
DB_PORT=5432                              # Database port
DB_NAME=musicvista                        # Database name
DB_USER=your-db-username                  # Database username
DB_PASSWORD=your-db-password              # Database password

# Social login configuration (optional)
GOOGLE_CLIENT_ID=                # Google OAuth client ID
GOOGLE_CLIENT_SECRET=            # Google OAuth client secret
FACEBOOK_APP_ID=                 # Facebook app ID
FACEBOOK_APP_SECRET=             # Facebook app secret
GITHUB_CLIENT_ID=                # GitHub OAuth client ID
GITHUB_CLIENT_SECRET=            # GitHub OAuth client secret

# Other configuration
LOG_LEVEL=info                   # Log level: debug, info, warn, error
MAX_REQUEST_SIZE=10mb            # Maximum request body size
CORS_ORIGINS=http://localhost:5173  # Allowed CORS origins (separate multiple with commas)

# ===========================================
# 💡 Tips:
# 1. Use strong random secrets in production
#    Generation method: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# 2. Do not commit .env files with real secrets to public repositories
# 3. When deploying to cloud platforms, configure these values in platform environment variables
# ===========================================
